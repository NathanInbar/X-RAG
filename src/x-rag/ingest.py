

def main():
    ...
    # insert nodes into memgraph from our processed, distributable data

    # ENTITY NODE:
    # key (normalized subject/object surface), name (pick a surface from aliases: most common, first, etc),
    # 
    # RELATION:
    # key (normalized predicate surface), source_chunks (chunk_id list)

if __name__ == "__main__":
    main()